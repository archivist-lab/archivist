{
  "name": "archivist",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "bootstrap": "pnpm install && pnpm build",
    "docs:check": "node scripts/check-markdown-metadata.mjs && node scripts/check-documentation-drift.mjs",
    "build:packages": "pnpm -r --filter \"./packages/*\" build",
    "build:server": "pnpm --filter archivist-server build",
    "build:client": "pnpm --filter archivist-client build",
    "build:kodi": "python3 apps/kodi/build.py && python3 apps/kodi/repository/build.py",
    "build": "pnpm build:packages && pnpm build:server && pnpm build:client && pnpm build:player && pnpm build:catalogue && pnpm build:control-agent && pnpm build:control && pnpm build:kodi",
    "dev": "tsx watch --env-file=.env apps/server/src/server.ts",
    "dev:worker": "tsx watch --env-file=.env apps/server/src/worker.ts",
    "start": "node apps/server/dist/supervisor.js",
    "start:api": "node apps/server/dist/server.js",
    "start:worker": "node apps/server/dist/worker.js",
    "test:db": "pnpm --filter @archivist/db test",
    "test": "pnpm test:db && pnpm --filter archivist-server test && pnpm test:control",
    "test:player": "pnpm --filter archivist-player test",
    "test:kodi": "python3 -m unittest discover -s apps/kodi/tests -v",
    "lint": "node scripts/lint.mjs",
    "lint:fix": "biome lint --write apps/server/src client/src packages",
    "format": "biome format --write apps/server/src client/src packages",
    "typecheck": "pnpm build:packages && pnpm typecheck:only",
    "typecheck:only": "pnpm typecheck:server && pnpm typecheck:client && pnpm typecheck:control-agent && pnpm typecheck:control",
    "typecheck:server": "pnpm --filter archivist-server typecheck",
    "typecheck:client": "pnpm --filter archivist-client typecheck",
    "verify": "pnpm docs:check && pnpm lint && pnpm typecheck && pnpm build && pnpm test && pnpm test:player && pnpm test:kodi",
    "push": "bash scripts/push.sh",
    "build:player": "pnpm --filter archivist-player build",
    "build:catalogue": "pnpm --filter archivist-catalogue build",
    "build:control": "pnpm --filter archivist-control build",
    "build:control-agent": "pnpm --filter archivist-control-agent build",
    "dev:control": "pnpm --filter archivist-control dev",
    "typecheck:control": "pnpm --filter archivist-control typecheck",
    "typecheck:control-agent": "pnpm --filter archivist-control-agent typecheck",
    "test:control": "pnpm --filter archivist-control-agent test && pnpm --filter archivist-control test",
    "build:baremetal": "pnpm build:packages && pnpm build:server && pnpm build:client && pnpm build:player && pnpm build:catalogue && pnpm build:control-agent && pnpm build:control",
    "build:docker": "pnpm build:packages && pnpm build:server && pnpm build:client && pnpm build:player",
    "lint:all": "biome lint apps/server/src client/src packages",
    "test:performance": "pnpm --filter archivist-server exec tsx test/performance-safety.test.ts && pnpm --filter archivist-player exec vitest run test/performance-refresh.test.tsx && pnpm --filter archivist-server exec tsx test/performance-workload.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "tsx": "4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "utp-native"
    ]
  }
}
