{
  "name": "archivist",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "bootstrap": "corepack pnpm install && corepack pnpm build",
    "docs:check": "node scripts/check-markdown-metadata.mjs && node scripts/check-documentation-drift.mjs",
    "build:packages": "corepack pnpm -r --filter \"./packages/*\" build",
    "build:server": "corepack pnpm --filter archivist-server build",
    "build:client": "corepack pnpm --filter archivist-client build",
    "build:kodi": "python3 apps/kodi/build.py && python3 apps/kodi/repository/build.py",
    "build": "corepack pnpm build:packages && corepack pnpm build:server && corepack pnpm build:client && corepack pnpm build:player && corepack pnpm build:catalogue && corepack pnpm build:control-agent && corepack pnpm build:control && corepack pnpm build:kodi",
    "dev": "tsx watch --env-file=.env apps/server/src/server.ts",
    "dev:worker": "tsx watch --env-file=.env apps/server/src/worker.ts",
    "start": "node apps/server/dist/supervisor.js",
    "start:api": "node apps/server/dist/server.js",
    "start:worker": "node apps/server/dist/worker.js",
    "test:db": "corepack pnpm --filter @archivist/db test",
    "test": "corepack pnpm test:db && corepack pnpm --filter archivist-server test && corepack pnpm test:control",
    "test:player": "corepack pnpm --filter archivist-player test",
    "test:kodi": "python3 -m unittest discover -s apps/kodi/tests -v",
    "lint": "biome lint apps/server/src client/src packages",
    "lint:fix": "biome lint --write apps/server/src client/src packages",
    "format": "biome format --write apps/server/src client/src packages",
    "typecheck": "corepack pnpm build:packages && corepack pnpm typecheck:only",
    "typecheck:only": "corepack pnpm typecheck:server && corepack pnpm typecheck:client && corepack pnpm typecheck:control-agent && corepack pnpm typecheck:control",
    "typecheck:server": "corepack pnpm --filter archivist-server typecheck",
    "typecheck:client": "corepack pnpm --filter archivist-client typecheck",
    "verify": "corepack pnpm docs:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm build && corepack pnpm test && corepack pnpm test:player && corepack pnpm test:kodi",
    "push": "bash scripts/push.sh",
    "build:player": "corepack pnpm --filter archivist-player build",
    "build:catalogue": "corepack pnpm --filter archivist-catalogue build",
    "build:control": "corepack pnpm --filter archivist-control build",
    "build:control-agent": "corepack pnpm --filter archivist-control-agent build",
    "dev:control": "corepack pnpm --filter archivist-control dev",
    "typecheck:control": "corepack pnpm --filter archivist-control typecheck",
    "typecheck:control-agent": "corepack pnpm --filter archivist-control-agent typecheck",
    "test:control": "corepack pnpm --filter archivist-control-agent test && corepack pnpm --filter archivist-control test",
    "build:baremetal": "corepack pnpm build:packages && corepack pnpm build:server && corepack pnpm build:client && corepack pnpm build:player && corepack pnpm build:catalogue && corepack pnpm build:control-agent && corepack pnpm build:control",
    "build:docker": "corepack pnpm build:packages && corepack pnpm build:server && corepack pnpm build:client && corepack pnpm build:player"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "tsx": "4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "utp-native"
    ]
  }
}
